# f1-database-project
A relational database project built using SQL and Python to analyze Formula 1 race data, including drivers, constructors, races, and results. The project focuses on designing an efficient schema, enforcing data integrity through constraints, and performing analytical queries to extract meaningful insights from a real-world dataset,
